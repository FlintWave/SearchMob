# add-engine-status-visibility

Owner-only per-engine search outcome (contributed / empty / failed), computed on-device with no
telemetry, surfaced in the app and on the served page (loopback owner only), at parity with desktop.
