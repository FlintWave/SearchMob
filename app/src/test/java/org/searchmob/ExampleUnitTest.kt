package org.searchmob

import org.junit.Assert.assertEquals
import org.junit.Test

/** Trivial unit test proving the JVM unit-test harness runs. Replaced by real tests in later phases. */
class ExampleUnitTest {
    @Test
    fun arithmetic_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
